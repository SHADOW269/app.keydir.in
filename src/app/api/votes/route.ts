import { NextResponse } from 'next/server';

// Placeholder — will be implemented with auth + server actions
export async function GET() {
  return NextResponse.json({ message: 'Voting API — coming soon' });
}
